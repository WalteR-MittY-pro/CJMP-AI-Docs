# DatePicker

Date picker component and dialog, supports start-end range, lunar calendar, text style.

---

## PickerTextStyle
```
public class PickerTextStyle
```


## MyFont
```
public class MyFont
```


## DatePicker
```
public class DatePicker
```

- Main methods:`startDate(...)` / `endDate(...)` / `selected(...)` / `lunar(...)` / `onDateChange(callback)` / `onDateChangeAccept(callback)` / `disappearTextStyle(...)` / `textStyle(...)` / `selectedTextStyle(...)` etc.

## DatePickerDialog
Date picker dialog (popped up via static method), parameters include start/end/selected/lunar/onAccept/onCancel/onChange/style, etc.
