# View type defines (cj_view_typedef)

Common view-related type collection defines:dialog/menu/border/edge/drag/blur/Sheet options and helper classes.

> Search suggestion: locate by type name in the table below. For specific fields, refer to source.

---

## Dialogs and menus
- `PopupOptions` / `CustomPopupOptions` / `PopupMessageOptions` — popup dialog options
- `DismissPopupAction` — dismiss popup action
- `MenuElement` / `MenuOptions` / `ContextMenuOptions` / `ContextMenuAnimationOptions` / `BindOptions` — menu element/options/context menu
- `Action` / `StateChangeEvent` — action/state change event

## Sheet and content overlay
- `SheetOptions` / `SheetTitleOptions` / `SheetDismiss` / `DismissSheetAction` — semi-modal Sheet options
- `SheetSize` / `SheetType` / `SheetMode` — Sheet size/type/mode
- `ContentCoverOptions` / `DismissContentCoverAction` — full-screen content overlay
- `SpringBackAction` — spring back action

## Border and edge
- `EdgeWidths` / `Edges` / `EdgeColors` / `EdgeOutlineStyles` / `EdgeOutlineWidths` / `OutlineRadiuses` — per-edge styles
- `LocalizedEdgeColors` / `LocalizedEdgeWidths` / `LocalizedBorderRadiuses` — localized (RTL) edge styles
- `DirectionalEdgesT<T>` — directional edge generic
- `BorderImageOption` — border image
- `Rectangle` — rectangle (x/y/width/height)

## Blur and effect
- `BlurOptions` / `BlurStyleOptions` / `BackgroundBlurStyleOptions` / `ForegroundBlurStyleOptions` — blur options
- `BackgroundEffectOptions` — background effect
- `LinearGradientBlurOptions` / `LinearGradientOption` — linear gradient blur/gradient

## Drag and scroll
- `DragInteractionOptions` — drag interaction options
- `DragPreviewMode` — drag preview mode
- `ContentOffset` — content offset
- `ScrollSizeMode` — scroll size mode

## Enums
- `ModalTransition` — modal transition
- `RepeatMode` — repeat mode
