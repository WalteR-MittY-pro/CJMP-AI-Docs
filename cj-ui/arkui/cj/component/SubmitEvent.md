# SubmitEvent

public class SubmitEvent

input box submit event，carries extension info for onSubmit callback。

## Methods
- `text()` — gets the submit text
- `keepKeyboard(value)` — whether to keep the keyboard after submit
