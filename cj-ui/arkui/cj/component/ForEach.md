# ForEach

public struct ForEach <: ComponentRender

Loop render control flow, builds child components by iterating over each item in the data array.

> Note: Not annotated with `@APILevel` metadata in source. Generated by the compiler at `ForEach` call sites, used indirectly by users via `ForEach(arr, item => ...)`.
