# __Recycle__

public class __Recycle__ <: ContainerBase

Reuse container internal implementation, works with the @Reusable macro to implement list item reuse.

> This is internal infrastructure, generated and used by the `@Reusable` macro; users do not instantiate it directly.
