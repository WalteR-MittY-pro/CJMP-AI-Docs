# View

public open class View <: ViewBase

One of the view base classes, provides custom view building process support. Inherits from ViewBase; users typically use CustomView rather than inheriting View directly.

> Note:Not annotated with `@APILevel` metadata in source. This is internal infrastructure.
