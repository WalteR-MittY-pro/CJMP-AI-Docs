# ComponentBase

Component interface base class, defines the rendering pipeline contract.

---

## ComponentRender
```
public interface ComponentRender
```


## ComponentBase
```
public interface ComponentBase<T> <: ComponentRender
```


> This is internal infrastructure; users indirectly use it via the `@Component` / `@Entry` macros and do not implement it directly.
