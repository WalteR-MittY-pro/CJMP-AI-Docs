# ContextMenu

Context menu controller, provides a static method to close the current context menu.

> Note:The public symbols of this component are not annotated with `@APILevel` metadata in source, so APILevel/syscap rows are omitted in the sections below.

---

## close
```
public static func close(): Unit
```

- description:Closes the currently-displayed context menu.
