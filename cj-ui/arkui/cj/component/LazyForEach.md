# LazyForEach

public class LazyForEach <: ComponentRender

Lazy loading loop render control stream, only builds visible area child components, suitable for long lists.

> Note: Not annotated with `@APILevel` metadata in source. Generated by the compiler at the `LazyForEach` call site; the data source must implement IDataSource and notify changes via DataChangeListener, see [cj_data_source](./cj_data_source.md).
