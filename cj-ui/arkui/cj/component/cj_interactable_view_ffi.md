# Interaction Event FFI (cj_interactable_view_ffi)

touch test related FFI struct and vector wrapper，internal infrastructure。

> Contains `CJTouchTestInfo` / `CTouchResult` / `FFIVectorTouchTestInfo` etc., all for internal FFI management, does not provide a user-facing public API.
