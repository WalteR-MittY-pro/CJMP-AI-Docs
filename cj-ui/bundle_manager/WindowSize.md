# WindowSize

Window size constraint struct (max/min width and height and ratio).

---

## API

### init
```
init(ret: RetWindowSize)
```

- description:Constructs an instance with the given parameters.

## Fields

- maxWindowRatio:Float64
- minWindowRatio:Float64
- maxWindowWidth:UInt32
- minWindowWidth:UInt32
- maxWindowHeight:UInt32
- minWindowHeight:UInt32
